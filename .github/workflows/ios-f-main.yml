name: Build Captain iOS (disabled)

on:
  workflow_dispatch: # manual only — use build-captain-ios.yml instead

jobs:
  build-ios:
    runs-on: macos-latest
    steps:
      - run: echo "This workflow is disabled. Use build-captain-ios.yml."
