name: Build Captain APK (disabled)

on:
  workflow_dispatch: # manual only — use build-captain-apk.yml instead

jobs:
  build-apk:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled. Use build-captain-apk.yml."
