name: Build Captain APK

on:
  push:
    branches: [main]
    paths:
      - 'client/src/**'
      - 'client/vite.captain.config.js'
      - 'client/index-captain.html'
      - '.github/workflows/build-captain-apk.yml'
  workflow_dispatch: # allow manual trigger

jobs:
  build-apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: client/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: client

      - name: Build captain web app
        run: npm run build:captain
        working-directory: client

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Swap to captain capacitor config
        run: |
          cp client/capacitor.config.json client/capacitor.config.backup.json
          cp client/capacitor.captain.json client/capacitor.config.json

      - name: Sync Capacitor
        run: npx cap sync android
        working-directory: client

      - name: Restore original capacitor config
        run: cp client/capacitor.config.backup.json client/capacitor.config.json

      - name: Make gradlew executable
        run: chmod +x gradlew
        working-directory: client/android

      - name: Build debug APK
        run: ./gradlew assembleDebug --no-daemon
        working-directory: client/android

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: WaitNot-Captain-APK
          path: client/android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 30
