platform :ios, '15.0'
use_frameworks!

project 'App.xcodeproj'

install! 'cocoapods', :disable_input_output_paths => true

target 'App' do
  # Firebase for push notifications only — Capacitor comes via SPM
  pod 'Firebase/Core'
  pod 'Firebase/Messaging'
end
